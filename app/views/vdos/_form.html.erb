<%= form_with(model: vdo, local: true) do |form| %>
  <% if vdo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vdo.errors.count, "error") %> prohibited this vdo from being saved:</h2>

      <ul>
      <% vdo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
